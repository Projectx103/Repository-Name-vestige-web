{
  "name": "vestige-functions",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "serve": "npm run build && firebase emulators:start --only functions",
    "test": "npx firebase-tools emulators:exec --project demo-vestige --only auth,firestore \"vitest run\""
  },
  "dependencies": {
    "cloudinary": "^2.5.1",
    "firebase-admin": "^13.0.1",
    "firebase-functions": "^6.1.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.16.0",
    "eslint": "^9.16.0",
    "firebase-functions-test": "^3.4.0",
    "globals": "^15.13.0",
    "typescript": "~5.6.3",
    "typescript-eslint": "^8.17.0",
    "vitest": "^2.1.8"
  }
}
