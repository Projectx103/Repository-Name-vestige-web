/** The role enum, matching custom claim values (06 - User Roles.md). */
export type {
  UserRole,
  GrantableRole,
  RoleGrantDocument,
  RoleGrantInput,
} from '@vestige/shared-schemas';
export { userRoleSchema, grantableRoleSchema, roleGrantInputSchema } from '@vestige/shared-schemas';
